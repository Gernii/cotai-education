{
  "name": "cotai-education",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "validate": "run-s \"validate.*\"",
    "validate.lint": "turbo run lint.fix --parallel",
    "validate.fmt": "turbo run fmt.fix --parallel",
    
    "dev": "turbo run dev",

    "lint": "turbo run lint.check --parallel",
    "test": "turbo run test --parallel",
    "fmt": "turbo run fmt.check --parallel",
    "check": "turbo run check --parallel",
    "deploy": "turbo run deploy --parallel",
    
    "commit": "czg"

  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.7",
    "npm-run-all2": "^6.2.0",
    "prettier": "^3.3.2",
    "turbo": "latest",
    "typescript": "^5.5.2",
    "czg": "1.9.3",
    "@biomejs/biome": "^1.8.2"
  },
  "packageManager": "npm@10.2.3",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/web",
    "packages/eslint-config",
    "packages/types-bridge"
  ]
}
